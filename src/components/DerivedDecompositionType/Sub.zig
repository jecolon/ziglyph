// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Sub code points.

const std = @import("std");
const mem = std.mem;

const Sub = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 7522,
hi: u21 = 11388,

pub fn init(allocator: *mem.Allocator) !Sub {
    var instance = Sub{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 3867),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 8) : (index += 1) {
        instance.array[index] = true;
    }
    index = 798;
    while (index <= 807) : (index += 1) {
        instance.array[index] = true;
    }
    index = 808;
    while (index <= 810) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[811] = true;
    instance.array[812] = true;
    index = 814;
    while (index <= 826) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[3866] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Sub) void {
    self.allocator.free(self.array);
}

// isSub checks if cp is of the kind Sub.
pub fn isSub(self: Sub, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
