// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Medefaidrin code points.

const std = @import("std");
const mem = std.mem;

const Medefaidrin = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 93760,
hi: u21 = 93850,

pub fn init(allocator: *mem.Allocator) !Medefaidrin {
    var instance = Medefaidrin{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 91),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 63) : (index += 1) {
        instance.array[index] = true;
    }
    index = 64;
    while (index <= 86) : (index += 1) {
        instance.array[index] = true;
    }
    index = 87;
    while (index <= 90) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Medefaidrin) void {
    self.allocator.free(self.array);
}

// isMedefaidrin checks if cp is of the kind Medefaidrin.
pub fn isMedefaidrin(self: Medefaidrin, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
