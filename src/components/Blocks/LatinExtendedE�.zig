// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Latin Extended-E code points.

const std = @import("std");
const mem = std.mem;

const LatinExtendedEª = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 43887,

pub fn init(allocator: *mem.Allocator) !LatinExtendedEª {
    var instance = LatinExtendedEª{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 43888),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 43824;
    while (index <= 43887) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *LatinExtendedEª) void {
    self.allocator.free(self.array);
}

// isLatinExtendedEª checks if cp is of the kind Latin Extended-E.
pub fn isLatinExtendedEª(self: LatinExtendedEª, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
