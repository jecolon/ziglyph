// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Pahawh Hmong code points.

const std = @import("std");
const mem = std.mem;

const PahawhHmong = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 93071,

pub fn init(allocator: *mem.Allocator) !PahawhHmong {
    var instance = PahawhHmong{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 93072),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 92928;
    while (index <= 93071) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *PahawhHmong) void {
    self.allocator.free(self.array);
}

// isPahawhHmong checks if cp is of the kind Pahawh Hmong.
pub fn isPahawhHmong(self: PahawhHmong, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
