// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode T code points.

const std = @import("std");
const mem = std.mem;

const T = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 44032,
hi: u21 = 55291,

pub fn init(allocator: *mem.Allocator) !T {
    var instance = T{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 11260),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2057640;
    while (index <= 2057727) : (index += 1) {
        instance.array[index] = true;
    }
    index = 11211;
    while (index <= 11259) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *T) void {
    self.allocator.free(self.array);
}

// isT checks if cp is of the kind T.
pub fn isT(self: T, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
