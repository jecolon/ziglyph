// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Caucasian_Albanian code points.

const std = @import("std");
const mem = std.mem;

const CaucasianAlbanian = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 66864,
hi: u21 = 66927,

pub fn init(allocator: *mem.Allocator) !CaucasianAlbanian {
    var instance = CaucasianAlbanian{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 64),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 51) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[63] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *CaucasianAlbanian) void {
    self.allocator.free(self.array);
}

// isCaucasianAlbanian checks if cp is of the kind Caucasian_Albanian.
pub fn isCaucasianAlbanian(self: CaucasianAlbanian, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
