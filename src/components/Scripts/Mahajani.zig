// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Mahajani code points.

const std = @import("std");
const mem = std.mem;

const Mahajani = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 69968,
hi: u21 = 70006,

pub fn init(allocator: *mem.Allocator) !Mahajani {
    var instance = Mahajani{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 39),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 34) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[35] = true;
    index = 36;
    while (index <= 37) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[38] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Mahajani) void {
    self.allocator.free(self.array);
}

// isMahajani checks if cp is of the kind Mahajani.
pub fn isMahajani(self: Mahajani, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
