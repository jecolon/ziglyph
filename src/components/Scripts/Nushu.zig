// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Nushu code points.

const std = @import("std");
const mem = std.mem;

const Nushu = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 72272,
hi: u21 = 111355,

pub fn init(allocator: *mem.Allocator) !Nushu {
    var instance = Nushu{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 39084),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    instance.array[21905] = true;
    index = 38688;
    while (index <= 39083) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Nushu) void {
    self.allocator.free(self.array);
}

// isNushu checks if cp is of the kind Nushu.
pub fn isNushu(self: Nushu, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
