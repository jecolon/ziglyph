// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Supplemental Arrows-C code points.

const std = @import("std");
const mem = std.mem;

const SupplementalArrowsCª = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 129279,

pub fn init(allocator: *mem.Allocator) !SupplementalArrowsCª {
    var instance = SupplementalArrowsCª{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 129280),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 129024;
    while (index <= 129279) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *SupplementalArrowsCª) void {
    self.allocator.free(self.array);
}

// isSupplementalArrowsCª checks if cp is of the kind Supplemental Arrows-C.
pub fn isSupplementalArrowsCª(self: SupplementalArrowsCª, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
