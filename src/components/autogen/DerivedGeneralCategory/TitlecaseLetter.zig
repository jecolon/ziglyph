// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Lowest code point
//    3. Highest code point
//! Unicode Titlecase_Letter code points.

const std = @import("std");
const mem = std.mem;

const TitlecaseLetter = @This();

allocator: *mem.Allocator,
cp_set: std.AutoHashMap(u21, void),
lo: u21 = 453,
hi: u21 = 8188,

pub fn init(allocator: *mem.Allocator) !TitlecaseLetter {
    var instance = TitlecaseLetter{
        .allocator = allocator,
        .cp_set = std.AutoHashMap(u21, void).init(allocator),
    };

    var index: u21 = 0;
    try instance.cp_set.put(453, {});
    try instance.cp_set.put(456, {});
    try instance.cp_set.put(459, {});
    try instance.cp_set.put(498, {});
    index = 8072;
    while (index <= 8079) : (index += 1) {
        try instance.cp_set.put(index, {});
    }
    index = 8088;
    while (index <= 8095) : (index += 1) {
        try instance.cp_set.put(index, {});
    }
    index = 8104;
    while (index <= 8111) : (index += 1) {
        try instance.cp_set.put(index, {});
    }
    try instance.cp_set.put(8124, {});
    try instance.cp_set.put(8140, {});
    try instance.cp_set.put(8188, {});

    // Placeholder: 0. Struct name, 1. Code point kind
    return instance;
}

pub fn deinit(self: *TitlecaseLetter) void {
    self.cp_set.deinit();
}

// isTitlecaseLetter checks if cp is of the kind Titlecase_Letter.
pub fn isTitlecaseLetter(self: TitlecaseLetter, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    return self.cp_set.get(cp) != null;
}
