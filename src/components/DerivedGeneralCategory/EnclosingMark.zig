// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Enclosing_Mark code points.

const std = @import("std");
const mem = std.mem;

const EnclosingMark = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 1160,
hi: u21 = 42610,

pub fn init(allocator: *mem.Allocator) !EnclosingMark {
    var instance = EnclosingMark{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 41451),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 1) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[5686] = true;
    index = 7253;
    while (index <= 7256) : (index += 1) {
        instance.array[index] = true;
    }
    index = 7258;
    while (index <= 7260) : (index += 1) {
        instance.array[index] = true;
    }
    index = 41448;
    while (index <= 41450) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *EnclosingMark) void {
    self.allocator.free(self.array);
}

// isEnclosingMark checks if cp is of the kind Enclosing_Mark.
pub fn isEnclosingMark(self: EnclosingMark, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
