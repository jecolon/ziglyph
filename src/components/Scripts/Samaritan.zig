// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Samaritan code points.

const std = @import("std");
const mem = std.mem;

const Samaritan = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 2070,
hi: u21 = 42231,

pub fn init(allocator: *mem.Allocator) !Samaritan {
    var instance = Samaritan{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 40162),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097130;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }
    index = 0;
    while (index <= 3) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[4] = true;
    index = 5;
    while (index <= 13) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[14] = true;
    index = 15;
    while (index <= 17) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[18] = true;
    index = 19;
    while (index <= 23) : (index += 1) {
        instance.array[index] = true;
    }
    index = 26;
    while (index <= 40) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Samaritan) void {
    self.allocator.free(self.array);
}

// isSamaritan checks if cp is of the kind Samaritan.
pub fn isSamaritan(self: Samaritan, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
