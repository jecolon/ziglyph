// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Supplemental Arrows-A code points.

const std = @import("std");
const mem = std.mem;

const SupplementalArrowsAs = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 10240,
hi: u21 = 10495,

pub fn init(allocator: *mem.Allocator) !SupplementalArrowsAs {
    var instance = SupplementalArrowsAs{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 256),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097136;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *SupplementalArrowsAs) void {
    self.allocator.free(self.array);
}

// isSupplementalArrowsAs checks if cp is of the kind Supplemental Arrows-A.
pub fn isSupplementalArrowsAs(self: SupplementalArrowsAs, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
