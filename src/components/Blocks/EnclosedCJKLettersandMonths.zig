// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Enclosed CJK Letters and Months code points.

const std = @import("std");
const mem = std.mem;

const EnclosedCJKLettersandMonths = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 12800,
hi: u21 = 13055,

pub fn init(allocator: *mem.Allocator) !EnclosedCJKLettersandMonths {
    var instance = EnclosedCJKLettersandMonths{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 256),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 255) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *EnclosedCJKLettersandMonths) void {
    self.allocator.free(self.array);
}

// isEnclosedCJKLettersandMonths checks if cp is of the kind Enclosed CJK Letters and Months.
pub fn isEnclosedCJKLettersandMonths(self: EnclosedCJKLettersandMonths, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
