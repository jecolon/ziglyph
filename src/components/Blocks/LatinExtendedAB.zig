// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Latin Extended-A code points.

const std = @import("std");
const mem = std.mem;

const LatinExtendedAB = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 384,
hi: u21 = 591,

pub fn init(allocator: *mem.Allocator) !LatinExtendedAB {
    var instance = LatinExtendedAB{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 208),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097024;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *LatinExtendedAB) void {
    self.allocator.free(self.array);
}

// isLatinExtendedAB checks if cp is of the kind Latin Extended-A.
pub fn isLatinExtendedAB(self: LatinExtendedAB, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
