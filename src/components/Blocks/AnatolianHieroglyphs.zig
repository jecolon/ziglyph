// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Anatolian Hieroglyphs code points.

const std = @import("std");
const mem = std.mem;

const AnatolianHieroglyphs = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 83583,

pub fn init(allocator: *mem.Allocator) !AnatolianHieroglyphs {
    var instance = AnatolianHieroglyphs{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 83584),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 82944;
    while (index <= 83583) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *AnatolianHieroglyphs) void {
    self.allocator.free(self.array);
}

// isAnatolianHieroglyphs checks if cp is of the kind Anatolian Hieroglyphs.
pub fn isAnatolianHieroglyphs(self: AnatolianHieroglyphs, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
