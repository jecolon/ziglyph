// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Lycian code points.

const std = @import("std");
const mem = std.mem;

const Lycian = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 66208,
hi: u21 = 66256,

pub fn init(allocator: *mem.Allocator) !Lycian {
    var instance = Lycian{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 49),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097120;
    while (index <= 2097148) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Lycian) void {
    self.allocator.free(self.array);
}

// isLycian checks if cp is of the kind Lycian.
pub fn isLycian(self: Lycian, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
