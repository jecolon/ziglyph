// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Nyiakeng_Puachue_Hmong code points.

const std = @import("std");
const mem = std.mem;

const NyiakengPuachueHmong = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 123184,
hi: u21 = 123627,

pub fn init(allocator: *mem.Allocator) !NyiakengPuachueHmong {
    var instance = NyiakengPuachueHmong{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 444),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097104;
    while (index <= 2097148) : (index += 1) {
        instance.array[index] = true;
    }
    index = 0;
    while (index <= 6) : (index += 1) {
        instance.array[index] = true;
    }
    index = 7;
    while (index <= 13) : (index += 1) {
        instance.array[index] = true;
    }
    index = 16;
    while (index <= 25) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[30] = true;
    instance.array[31] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *NyiakengPuachueHmong) void {
    self.allocator.free(self.array);
}

// isNyiakengPuachueHmong checks if cp is of the kind Nyiakeng_Puachue_Hmong.
pub fn isNyiakengPuachueHmong(self: NyiakengPuachueHmong, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
