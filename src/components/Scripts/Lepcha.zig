// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Lepcha code points.

const std = @import("std");
const mem = std.mem;

const Lepcha = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 7204,
hi: u21 = 7257,

pub fn init(allocator: *mem.Allocator) !Lepcha {
    var instance = Lepcha{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 54),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097116;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }
    index = 0;
    while (index <= 7) : (index += 1) {
        instance.array[index] = true;
    }
    index = 8;
    while (index <= 15) : (index += 1) {
        instance.array[index] = true;
    }
    index = 16;
    while (index <= 17) : (index += 1) {
        instance.array[index] = true;
    }
    index = 18;
    while (index <= 19) : (index += 1) {
        instance.array[index] = true;
    }
    index = 23;
    while (index <= 27) : (index += 1) {
        instance.array[index] = true;
    }
    index = 28;
    while (index <= 37) : (index += 1) {
        instance.array[index] = true;
    }
    index = 41;
    while (index <= 43) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Lepcha) void {
    self.allocator.free(self.array);
}

// isLepcha checks if cp is of the kind Lepcha.
pub fn isLepcha(self: Lepcha, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
