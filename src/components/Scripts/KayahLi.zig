// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Kayah_Li code points.

const std = @import("std");
const mem = std.mem;

const KayahLi = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 43274,
hi: u21 = 43334,

pub fn init(allocator: *mem.Allocator) !KayahLi {
    var instance = KayahLi{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 61),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097142;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }
    index = 0;
    while (index <= 27) : (index += 1) {
        instance.array[index] = true;
    }
    index = 28;
    while (index <= 35) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[37] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *KayahLi) void {
    self.allocator.free(self.array);
}

// isKayahLi checks if cp is of the kind Kayah_Li.
pub fn isKayahLi(self: KayahLi, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
