// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Inscriptional_Parthian code points.

const std = @import("std");
const mem = std.mem;

const InscriptionalParthian = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 68440,
hi: u21 = 68466,

pub fn init(allocator: *mem.Allocator) !InscriptionalParthian {
    var instance = InscriptionalParthian{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 27),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097128;
    while (index <= 2097149) : (index += 1) {
        instance.array[index] = true;
    }
    index = 0;
    while (index <= 7) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *InscriptionalParthian) void {
    self.allocator.free(self.array);
}

// isInscriptionalParthian checks if cp is of the kind Inscriptional_Parthian.
pub fn isInscriptionalParthian(self: InscriptionalParthian, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
