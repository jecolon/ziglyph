// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode CJK Compatibility Ideographs code points.

const std = @import("std");
const mem = std.mem;

const CJKCompatibilityIdeographs = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 64255,

pub fn init(allocator: *mem.Allocator) !CJKCompatibilityIdeographs {
    var instance = CJKCompatibilityIdeographs{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 64256),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 63744;
    while (index <= 64255) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *CJKCompatibilityIdeographs) void {
    self.allocator.free(self.array);
}

// isCJKCompatibilityIdeographs checks if cp is of the kind CJK Compatibility Ideographs.
pub fn isCJKCompatibilityIdeographs(self: CJKCompatibilityIdeographs, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
