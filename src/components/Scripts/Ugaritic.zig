// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Ugaritic code points.

const std = @import("std");
const mem = std.mem;

const Ugaritic = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 66463,
hi: u21 = 66687,

pub fn init(allocator: *mem.Allocator) !Ugaritic {
    var instance = Ugaritic{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 225),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097121;
    while (index <= 2097150) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[0] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Ugaritic) void {
    self.allocator.free(self.array);
}

// isUgaritic checks if cp is of the kind Ugaritic.
pub fn isUgaritic(self: Ugaritic, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
