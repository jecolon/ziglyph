// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Latin Extended Additional code points.

const std = @import("std");
const mem = std.mem;

const LatinExtendedAdditional = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 7680,
hi: u21 = 7935,

pub fn init(allocator: *mem.Allocator) !LatinExtendedAdditional {
    var instance = LatinExtendedAdditional{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 256),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 255) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *LatinExtendedAdditional) void {
    self.allocator.free(self.array);
}

// isLatinExtendedAdditional checks if cp is of the kind Latin Extended Additional.
pub fn isLatinExtendedAdditional(self: LatinExtendedAdditional, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
