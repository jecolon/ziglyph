// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Kaithi code points.

const std = @import("std");
const mem = std.mem;

const Kaithi = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 7104,
hi: u21 = 69837,

pub fn init(allocator: *mem.Allocator) !Kaithi {
    var instance = Kaithi{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 62734),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 62656;
    while (index <= 62657) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[62658] = true;
    index = 62659;
    while (index <= 62703) : (index += 1) {
        instance.array[index] = true;
    }
    index = 62704;
    while (index <= 62706) : (index += 1) {
        instance.array[index] = true;
    }
    index = 62707;
    while (index <= 62710) : (index += 1) {
        instance.array[index] = true;
    }
    index = 62711;
    while (index <= 62712) : (index += 1) {
        instance.array[index] = true;
    }
    index = 62713;
    while (index <= 62714) : (index += 1) {
        instance.array[index] = true;
    }
    index = 62715;
    while (index <= 62716) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[62717] = true;
    index = 62718;
    while (index <= 62721) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[62733] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Kaithi) void {
    self.allocator.free(self.array);
}

// isKaithi checks if cp is of the kind Kaithi.
pub fn isKaithi(self: Kaithi, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
