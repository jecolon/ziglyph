
    // Placeholder: Struct name.
    return instance;
}}

pub fn is{s}(self: {0s}, cp: u21) bool {{
    return if (cp >= self.array.len or cp < self.lo or cp > self.hi) false else self.array[cp];
}}
