// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Symbols for Legacy Computing code points.

const std = @import("std");
const mem = std.mem;

const SymbolsforLegacyComputing = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 130047,

pub fn init(allocator: *mem.Allocator) !SymbolsforLegacyComputing {
    var instance = SymbolsforLegacyComputing{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 130048),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 129792;
    while (index <= 130047) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *SymbolsforLegacyComputing) void {
    self.allocator.free(self.array);
}

// isSymbolsforLegacyComputing checks if cp is of the kind Symbols for Legacy Computing.
pub fn isSymbolsforLegacyComputing(self: SymbolsforLegacyComputing, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
