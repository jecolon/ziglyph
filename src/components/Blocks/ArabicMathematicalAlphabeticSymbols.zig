// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Arabic Mathematical Alphabetic Symbols code points.

const std = @import("std");
const mem = std.mem;

const ArabicMathematicalAlphabeticSymbols = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 126464,
hi: u21 = 126719,

pub fn init(allocator: *mem.Allocator) !ArabicMathematicalAlphabeticSymbols {
    var instance = ArabicMathematicalAlphabeticSymbols{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 256),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 255) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *ArabicMathematicalAlphabeticSymbols) void {
    self.allocator.free(self.array);
}

// isArabicMathematicalAlphabeticSymbols checks if cp is of the kind Arabic Mathematical Alphabetic Symbols.
pub fn isArabicMathematicalAlphabeticSymbols(self: ArabicMathematicalAlphabeticSymbols, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
