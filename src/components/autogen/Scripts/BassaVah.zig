// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Bassa_Vah code points.

const std = @import("std");
const mem = std.mem;

const BassaVah = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 92880,
hi: u21 = 92917,

pub fn init(allocator: *mem.Allocator) !BassaVah {
    var instance = BassaVah{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 38),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 29) : (index += 1) {
        instance.array[index] = true;
    }
    index = 32;
    while (index <= 36) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[37] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *BassaVah) void {
    self.allocator.free(self.array);
}

// isBassaVah checks if cp is of the kind Bassa_Vah.
pub fn isBassaVah(self: BassaVah, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
