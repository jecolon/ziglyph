// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Wancho code points.

const std = @import("std");
const mem = std.mem;

const Wancho = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 69552,
hi: u21 = 123647,

pub fn init(allocator: *mem.Allocator) !Wancho {
    var instance = Wancho{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 54096),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 54032;
    while (index <= 54075) : (index += 1) {
        instance.array[index] = true;
    }
    index = 54076;
    while (index <= 54079) : (index += 1) {
        instance.array[index] = true;
    }
    index = 54080;
    while (index <= 54089) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[54095] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Wancho) void {
    self.allocator.free(self.array);
}

// isWancho checks if cp is of the kind Wancho.
pub fn isWancho(self: Wancho, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
