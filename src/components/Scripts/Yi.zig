// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Yi code points.

const std = @import("std");
const mem = std.mem;

const Yi = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 40960,
hi: u21 = 42182,

pub fn init(allocator: *mem.Allocator) !Yi {
    var instance = Yi{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 1223),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 20) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[21] = true;
    index = 22;
    while (index <= 1164) : (index += 1) {
        instance.array[index] = true;
    }
    index = 1168;
    while (index <= 1222) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Yi) void {
    self.allocator.free(self.array);
}

// isYi checks if cp is of the kind Yi.
pub fn isYi(self: Yi, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
