// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Old_North_Arabian code points.

const std = @import("std");
const mem = std.mem;

const OldNorthArabian = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 67712,
hi: u21 = 68255,

pub fn init(allocator: *mem.Allocator) !OldNorthArabian {
    var instance = OldNorthArabian{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 544),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 512;
    while (index <= 540) : (index += 1) {
        instance.array[index] = true;
    }
    index = 541;
    while (index <= 543) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *OldNorthArabian) void {
    self.allocator.free(self.array);
}

// isOldNorthArabian checks if cp is of the kind Old_North_Arabian.
pub fn isOldNorthArabian(self: OldNorthArabian, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
