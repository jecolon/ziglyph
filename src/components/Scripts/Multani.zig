// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Multani code points.

const std = @import("std");
const mem = std.mem;

const Multani = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 68736,
hi: u21 = 70313,

pub fn init(allocator: *mem.Allocator) !Multani {
    var instance = Multani{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 1578),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 1536;
    while (index <= 1542) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[1544] = true;
    index = 1546;
    while (index <= 1549) : (index += 1) {
        instance.array[index] = true;
    }
    index = 1551;
    while (index <= 1565) : (index += 1) {
        instance.array[index] = true;
    }
    index = 1567;
    while (index <= 1576) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[1577] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Multani) void {
    self.allocator.free(self.array);
}

// isMultani checks if cp is of the kind Multani.
pub fn isMultani(self: Multani, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
