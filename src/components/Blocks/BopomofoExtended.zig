// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Bopomofo Extended code points.

const std = @import("std");
const mem = std.mem;

const BopomofoExtended = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 12735,

pub fn init(allocator: *mem.Allocator) !BopomofoExtended {
    var instance = BopomofoExtended{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 12736),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 12704;
    while (index <= 12735) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *BopomofoExtended) void {
    self.allocator.free(self.array);
}

// isBopomofoExtended checks if cp is of the kind Bopomofo Extended.
pub fn isBopomofoExtended(self: BopomofoExtended, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
