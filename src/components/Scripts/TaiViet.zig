// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Tai_Viet code points.

const std = @import("std");
const mem = std.mem;

const TaiViet = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 43696,
hi: u21 = 68405,

pub fn init(allocator: *mem.Allocator) !TaiViet {
    var instance = TaiViet{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 24710),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097104;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[0] = true;
    instance.array[1] = true;
    index = 2;
    while (index <= 4) : (index += 1) {
        instance.array[index] = true;
    }
    index = 5;
    while (index <= 6) : (index += 1) {
        instance.array[index] = true;
    }
    index = 7;
    while (index <= 8) : (index += 1) {
        instance.array[index] = true;
    }
    index = 9;
    while (index <= 13) : (index += 1) {
        instance.array[index] = true;
    }
    index = 14;
    while (index <= 15) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[16] = true;
    instance.array[17] = true;
    instance.array[18] = true;
    index = 43;
    while (index <= 44) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[45] = true;
    index = 46;
    while (index <= 47) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *TaiViet) void {
    self.allocator.free(self.array);
}

// isTaiViet checks if cp is of the kind Tai_Viet.
pub fn isTaiViet(self: TaiViet, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
