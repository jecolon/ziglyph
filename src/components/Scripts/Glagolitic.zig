// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Glagolitic code points.

const std = @import("std");
const mem = std.mem;

const Glagolitic = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 11264,
hi: u21 = 122922,

pub fn init(allocator: *mem.Allocator) !Glagolitic {
    var instance = Glagolitic{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 111659),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 46) : (index += 1) {
        instance.array[index] = true;
    }
    index = 48;
    while (index <= 94) : (index += 1) {
        instance.array[index] = true;
    }
    index = 111616;
    while (index <= 111622) : (index += 1) {
        instance.array[index] = true;
    }
    index = 111624;
    while (index <= 111640) : (index += 1) {
        instance.array[index] = true;
    }
    index = 111643;
    while (index <= 111649) : (index += 1) {
        instance.array[index] = true;
    }
    index = 111651;
    while (index <= 111652) : (index += 1) {
        instance.array[index] = true;
    }
    index = 111654;
    while (index <= 111658) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Glagolitic) void {
    self.allocator.free(self.array);
}

// isGlagolitic checks if cp is of the kind Glagolitic.
pub fn isGlagolitic(self: Glagolitic, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
