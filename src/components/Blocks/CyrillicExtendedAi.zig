// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Cyrillic Extended-A code points.

const std = @import("std");
const mem = std.mem;

const CyrillicExtendedAi = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 11776,
hi: u21 = 11903,

pub fn init(allocator: *mem.Allocator) !CyrillicExtendedAi {
    var instance = CyrillicExtendedAi{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 128),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097120;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *CyrillicExtendedAi) void {
    self.allocator.free(self.array);
}

// isCyrillicExtendedAi checks if cp is of the kind Cyrillic Extended-A.
pub fn isCyrillicExtendedAi(self: CyrillicExtendedAi, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
