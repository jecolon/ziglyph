// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Armenian code points.

const std = @import("std");
const mem = std.mem;

const Armenian = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 1329,
hi: u21 = 64279,

pub fn init(allocator: *mem.Allocator) !Armenian {
    var instance = Armenian{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 62951),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 37) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[40] = true;
    index = 41;
    while (index <= 46) : (index += 1) {
        instance.array[index] = true;
    }
    index = 47;
    while (index <= 87) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[88] = true;
    instance.array[89] = true;
    index = 92;
    while (index <= 93) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[94] = true;
    index = 62946;
    while (index <= 62950) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Armenian) void {
    self.allocator.free(self.array);
}

// isArmenian checks if cp is of the kind Armenian.
pub fn isArmenian(self: Armenian, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
