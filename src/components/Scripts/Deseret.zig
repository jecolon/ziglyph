// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Deseret code points.

const std = @import("std");
const mem = std.mem;

const Deseret = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 768,
hi: u21 = 879,

pub fn init(allocator: *mem.Allocator) !Deseret {
    var instance = Deseret{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 112),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 65792;
    while (index <= 65871) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Deseret) void {
    self.allocator.free(self.array);
}

// isDeseret checks if cp is of the kind Deseret.
pub fn isDeseret(self: Deseret, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
