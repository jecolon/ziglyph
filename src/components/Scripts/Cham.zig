// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Cham code points.

const std = @import("std");
const mem = std.mem;

const Cham = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 6688,
hi: u21 = 43615,

pub fn init(allocator: *mem.Allocator) !Cham {
    var instance = Cham{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 36928),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 36832;
    while (index <= 36872) : (index += 1) {
        instance.array[index] = true;
    }
    index = 36873;
    while (index <= 36878) : (index += 1) {
        instance.array[index] = true;
    }
    index = 36879;
    while (index <= 36880) : (index += 1) {
        instance.array[index] = true;
    }
    index = 36881;
    while (index <= 36882) : (index += 1) {
        instance.array[index] = true;
    }
    index = 36883;
    while (index <= 36884) : (index += 1) {
        instance.array[index] = true;
    }
    index = 36885;
    while (index <= 36886) : (index += 1) {
        instance.array[index] = true;
    }
    index = 36896;
    while (index <= 36898) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[36899] = true;
    index = 36900;
    while (index <= 36907) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[36908] = true;
    instance.array[36909] = true;
    index = 36912;
    while (index <= 36921) : (index += 1) {
        instance.array[index] = true;
    }
    index = 36924;
    while (index <= 36927) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Cham) void {
    self.allocator.free(self.array);
}

// isCham checks if cp is of the kind Cham.
pub fn isCham(self: Cham, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
