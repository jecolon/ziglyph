// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Ideographic Description Characters code points.

const std = @import("std");
const mem = std.mem;

const IdeographicDescriptionCharacters = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 12287,

pub fn init(allocator: *mem.Allocator) !IdeographicDescriptionCharacters {
    var instance = IdeographicDescriptionCharacters{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 12288),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 12272;
    while (index <= 12287) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *IdeographicDescriptionCharacters) void {
    self.allocator.free(self.array);
}

// isIdeographicDescriptionCharacters checks if cp is of the kind Ideographic Description Characters.
pub fn isIdeographicDescriptionCharacters(self: IdeographicDescriptionCharacters, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
