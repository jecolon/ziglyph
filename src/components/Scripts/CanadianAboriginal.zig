// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Canadian_Aboriginal code points.

const std = @import("std");
const mem = std.mem;

const CanadianAboriginal = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 5121,
hi: u21 = 6389,

pub fn init(allocator: *mem.Allocator) !CanadianAboriginal {
    var instance = CanadianAboriginal{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 1269),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    instance.array[2097151] = true;
    index = 0;
    while (index <= 619) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[620] = true;
    instance.array[621] = true;
    index = 622;
    while (index <= 638) : (index += 1) {
        instance.array[index] = true;
    }
    index = 1199;
    while (index <= 1268) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *CanadianAboriginal) void {
    self.allocator.free(self.array);
}

// isCanadianAboriginal checks if cp is of the kind Canadian_Aboriginal.
pub fn isCanadianAboriginal(self: CanadianAboriginal, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
