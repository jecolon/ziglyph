// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Brahmi code points.

const std = @import("std");
const mem = std.mem;

const Brahmi = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 2112,
hi: u21 = 69759,

pub fn init(allocator: *mem.Allocator) !Brahmi {
    var instance = Brahmi{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 67648),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    instance.array[67520] = true;
    instance.array[67521] = true;
    instance.array[67522] = true;
    index = 67523;
    while (index <= 67575) : (index += 1) {
        instance.array[index] = true;
    }
    index = 67576;
    while (index <= 67590) : (index += 1) {
        instance.array[index] = true;
    }
    index = 67591;
    while (index <= 67597) : (index += 1) {
        instance.array[index] = true;
    }
    index = 67602;
    while (index <= 67621) : (index += 1) {
        instance.array[index] = true;
    }
    index = 67622;
    while (index <= 67631) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[67647] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Brahmi) void {
    self.allocator.free(self.array);
}

// isBrahmi checks if cp is of the kind Brahmi.
pub fn isBrahmi(self: Brahmi, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
