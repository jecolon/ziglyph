// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Latin-1 Supplement code points.

const std = @import("std");
const mem = std.mem;

const Latin1Supplementa = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 256,
hi: u21 = 383,

pub fn init(allocator: *mem.Allocator) !Latin1Supplementa {
    var instance = Latin1Supplementa{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 128),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097024;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Latin1Supplementa) void {
    self.allocator.free(self.array);
}

// isLatin1Supplementa checks if cp is of the kind Latin-1 Supplement.
pub fn isLatin1Supplementa(self: Latin1Supplementa, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
