// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Hangul Jamo code points.

const std = @import("std");
const mem = std.mem;

const HangulJamo = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 4352,
hi: u21 = 4607,

pub fn init(allocator: *mem.Allocator) !HangulJamo {
    var instance = HangulJamo{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 256),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 255) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *HangulJamo) void {
    self.allocator.free(self.array);
}

// isHangulJamo checks if cp is of the kind Hangul Jamo.
pub fn isHangulJamo(self: HangulJamo, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
