// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Hanifi_Rohingya code points.

const std = @import("std");
const mem = std.mem;

const HanifiRohingya = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 68900,
hi: u21 = 69445,

pub fn init(allocator: *mem.Allocator) !HanifiRohingya {
    var instance = HanifiRohingya{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 546),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097116;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }
    index = 0;
    while (index <= 3) : (index += 1) {
        instance.array[index] = true;
    }
    index = 12;
    while (index <= 21) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *HanifiRohingya) void {
    self.allocator.free(self.array);
}

// isHanifiRohingya checks if cp is of the kind Hanifi_Rohingya.
pub fn isHanifiRohingya(self: HanifiRohingya, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
