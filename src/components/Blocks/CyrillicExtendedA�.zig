// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Cyrillic Extended-A code points.

const std = @import("std");
const mem = std.mem;

const CyrillicExtendedAª = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 11775,

pub fn init(allocator: *mem.Allocator) !CyrillicExtendedAª {
    var instance = CyrillicExtendedAª{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 11776),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 11744;
    while (index <= 11775) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *CyrillicExtendedAª) void {
    self.allocator.free(self.array);
}

// isCyrillicExtendedAª checks if cp is of the kind Cyrillic Extended-A.
pub fn isCyrillicExtendedAª(self: CyrillicExtendedAª, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
