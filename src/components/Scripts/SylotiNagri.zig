// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Syloti_Nagri code points.

const std = @import("std");
const mem = std.mem;

const SylotiNagri = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 43008,
hi: u21 = 43052,

pub fn init(allocator: *mem.Allocator) !SylotiNagri {
    var instance = SylotiNagri{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 45),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 1) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[2] = true;
    index = 3;
    while (index <= 5) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[6] = true;
    index = 7;
    while (index <= 10) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[11] = true;
    index = 12;
    while (index <= 34) : (index += 1) {
        instance.array[index] = true;
    }
    index = 35;
    while (index <= 36) : (index += 1) {
        instance.array[index] = true;
    }
    index = 37;
    while (index <= 38) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[39] = true;
    index = 40;
    while (index <= 43) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[44] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *SylotiNagri) void {
    self.allocator.free(self.array);
}

// isSylotiNagri checks if cp is of the kind Syloti_Nagri.
pub fn isSylotiNagri(self: SylotiNagri, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
