// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode CJK Unified Ideographs Extension B code points.

const std = @import("std");
const mem = std.mem;

const CJKUnifiedIdeographsExtensionB = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 131072,
hi: u21 = 173791,

pub fn init(allocator: *mem.Allocator) !CJKUnifiedIdeographsExtensionB {
    var instance = CJKUnifiedIdeographsExtensionB{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 42720),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 42719) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *CJKUnifiedIdeographsExtensionB) void {
    self.allocator.free(self.array);
}

// isCJKUnifiedIdeographsExtensionB checks if cp is of the kind CJK Unified Ideographs Extension B.
pub fn isCJKUnifiedIdeographsExtensionB(self: CJKUnifiedIdeographsExtensionB, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
