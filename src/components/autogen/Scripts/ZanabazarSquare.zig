// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Zanabazar_Square code points.

const std = @import("std");
const mem = std.mem;

const ZanabazarSquare = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 72192,
hi: u21 = 72263,

pub fn init(allocator: *mem.Allocator) !ZanabazarSquare {
    var instance = ZanabazarSquare{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 72),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    instance.array[0] = true;
    index = 1;
    while (index <= 10) : (index += 1) {
        instance.array[index] = true;
    }
    index = 11;
    while (index <= 50) : (index += 1) {
        instance.array[index] = true;
    }
    index = 51;
    while (index <= 56) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[57] = true;
    instance.array[58] = true;
    index = 59;
    while (index <= 62) : (index += 1) {
        instance.array[index] = true;
    }
    index = 63;
    while (index <= 70) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[71] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *ZanabazarSquare) void {
    self.allocator.free(self.array);
}

// isZanabazarSquare checks if cp is of the kind Zanabazar_Square.
pub fn isZanabazarSquare(self: ZanabazarSquare, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
