// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Mandaic code points.

const std = @import("std");
const mem = std.mem;

const Mandaic = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 2112,
hi: u21 = 2142,

pub fn init(allocator: *mem.Allocator) !Mandaic {
    var instance = Mandaic{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 31),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 24) : (index += 1) {
        instance.array[index] = true;
    }
    index = 25;
    while (index <= 27) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[30] = true;

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Mandaic) void {
    self.allocator.free(self.array);
}

// isMandaic checks if cp is of the kind Mandaic.
pub fn isMandaic(self: Mandaic, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
