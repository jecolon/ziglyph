// Placeholder: Code point type.

    return instance;
}}

const Self = @This();
pub fn deinit(self: *Self) void {{
    self.allocator.free(self.array);
}}

pub fn is{s}(self: Self, cp: u21) bool {{
    return if (cp >= self.array.len) false else self.array[cp];
}}
