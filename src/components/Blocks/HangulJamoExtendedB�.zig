// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Hangul Jamo Extended-B code points.

const std = @import("std");
const mem = std.mem;

const HangulJamoExtendedBª = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 55295,

pub fn init(allocator: *mem.Allocator) !HangulJamoExtendedBª {
    var instance = HangulJamoExtendedBª{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 55296),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 55216;
    while (index <= 55295) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *HangulJamoExtendedBª) void {
    self.allocator.free(self.array);
}

// isHangulJamoExtendedBª checks if cp is of the kind Hangul Jamo Extended-B.
pub fn isHangulJamoExtendedBª(self: HangulJamoExtendedBª, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
