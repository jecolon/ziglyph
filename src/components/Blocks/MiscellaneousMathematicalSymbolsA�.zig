// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Miscellaneous Mathematical Symbols-A code points.

const std = @import("std");
const mem = std.mem;

const MiscellaneousMathematicalSymbolsAª = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 10223,

pub fn init(allocator: *mem.Allocator) !MiscellaneousMathematicalSymbolsAª {
    var instance = MiscellaneousMathematicalSymbolsAª{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 10224),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 10176;
    while (index <= 10223) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *MiscellaneousMathematicalSymbolsAª) void {
    self.allocator.free(self.array);
}

// isMiscellaneousMathematicalSymbolsAª checks if cp is of the kind Miscellaneous Mathematical Symbols-A.
pub fn isMiscellaneousMathematicalSymbolsAª(self: MiscellaneousMathematicalSymbolsAª, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
