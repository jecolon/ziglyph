// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Combining Half Marks code points.

const std = @import("std");
const mem = std.mem;

const CombiningHalfMarks = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 65056,
hi: u21 = 65071,

pub fn init(allocator: *mem.Allocator) !CombiningHalfMarks {
    var instance = CombiningHalfMarks{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 16),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 15) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *CombiningHalfMarks) void {
    self.allocator.free(self.array);
}

// isCombiningHalfMarks checks if cp is of the kind Combining Half Marks.
pub fn isCombiningHalfMarks(self: CombiningHalfMarks, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
