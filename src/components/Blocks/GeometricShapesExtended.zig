// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Geometric Shapes Extended code points.

const std = @import("std");
const mem = std.mem;

const GeometricShapesExtended = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 129023,

pub fn init(allocator: *mem.Allocator) !GeometricShapesExtended {
    var instance = GeometricShapesExtended{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 129024),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 128896;
    while (index <= 129023) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *GeometricShapesExtended) void {
    self.allocator.free(self.array);
}

// isGeometricShapesExtended checks if cp is of the kind Geometric Shapes Extended.
pub fn isGeometricShapesExtended(self: GeometricShapesExtended, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
