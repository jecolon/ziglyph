// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Newa code points.

const std = @import("std");
const mem = std.mem;

const Newa = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 66736,
hi: u21 = 70753,

pub fn init(allocator: *mem.Allocator) !Newa {
    var instance = Newa{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 4018),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 3920;
    while (index <= 3972) : (index += 1) {
        instance.array[index] = true;
    }
    index = 3973;
    while (index <= 3975) : (index += 1) {
        instance.array[index] = true;
    }
    index = 3976;
    while (index <= 3983) : (index += 1) {
        instance.array[index] = true;
    }
    index = 3984;
    while (index <= 3985) : (index += 1) {
        instance.array[index] = true;
    }
    index = 3986;
    while (index <= 3988) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[3989] = true;
    instance.array[3990] = true;
    index = 3991;
    while (index <= 3994) : (index += 1) {
        instance.array[index] = true;
    }
    index = 3995;
    while (index <= 3999) : (index += 1) {
        instance.array[index] = true;
    }
    index = 4000;
    while (index <= 4009) : (index += 1) {
        instance.array[index] = true;
    }
    index = 4010;
    while (index <= 4011) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[4013] = true;
    instance.array[4014] = true;
    index = 4015;
    while (index <= 4017) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Newa) void {
    self.allocator.free(self.array);
}

// isNewa checks if cp is of the kind Newa.
pub fn isNewa(self: Newa, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
