// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Tagalog code points.

const std = @import("std");
const mem = std.mem;

const Tagalog = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 5902,
hi: u21 = 5937,

pub fn init(allocator: *mem.Allocator) !Tagalog {
    var instance = Tagalog{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 36),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097138;
    while (index <= 2097150) : (index += 1) {
        instance.array[index] = true;
    }
    index = 0;
    while (index <= 3) : (index += 1) {
        instance.array[index] = true;
    }
    index = 4;
    while (index <= 6) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Tagalog) void {
    self.allocator.free(self.array);
}

// isTagalog checks if cp is of the kind Tagalog.
pub fn isTagalog(self: Tagalog, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
