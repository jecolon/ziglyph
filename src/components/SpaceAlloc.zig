// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point kind
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Space category code points data.

const std = @import("std");
const mem = std.mem;
const Range = @import("../Range.zig");

const SpaceAlloc = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 32,
hi: u21 = 12288,

pub fn init(allocator: *mem.Allocator) !SpaceAlloc {
    var instance = SpaceAlloc{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 12257),
    };

    for (instance.array) |*item| {
        item.* = false;
    }

    instance.array[0] = true;
    instance.array[128] = true;
    instance.array[5728] = true;
    instance.array[8207] = true;
    instance.array[8255] = true;
    instance.array[12256] = true;

    var index: u21 = 0;
    index = 8160;
    while (index <= 8170) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Code point kind, 1. Struct name.
    return instance;
}

pub fn deinit(self: *SpaceAlloc) void {
    self.allocator.free(self.array);
}

pub fn isSpace(self: SpaceAlloc, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
