// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Miscellaneous Mathematical Symbols-B code points.

const std = @import("std");
const mem = std.mem;

const MiscellaneousMathematicalSymbolsBª = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 0,
hi: u21 = 10751,

pub fn init(allocator: *mem.Allocator) !MiscellaneousMathematicalSymbolsBª {
    var instance = MiscellaneousMathematicalSymbolsBª{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 10752),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 10624;
    while (index <= 10751) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *MiscellaneousMathematicalSymbolsBª) void {
    self.allocator.free(self.array);
}

// isMiscellaneousMathematicalSymbolsBª checks if cp is of the kind Miscellaneous Mathematical Symbols-B.
pub fn isMiscellaneousMathematicalSymbolsBª(self: MiscellaneousMathematicalSymbolsBª, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
