// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Meroitic_Cursive code points.

const std = @import("std");
const mem = std.mem;

const MeroiticCursive = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 68000,
hi: u21 = 68095,

pub fn init(allocator: *mem.Allocator) !MeroiticCursive {
    var instance = MeroiticCursive{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 96),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 23) : (index += 1) {
        instance.array[index] = true;
    }
    index = 28;
    while (index <= 29) : (index += 1) {
        instance.array[index] = true;
    }
    index = 30;
    while (index <= 31) : (index += 1) {
        instance.array[index] = true;
    }
    index = 32;
    while (index <= 47) : (index += 1) {
        instance.array[index] = true;
    }
    index = 50;
    while (index <= 95) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *MeroiticCursive) void {
    self.allocator.free(self.array);
}

// isMeroiticCursive checks if cp is of the kind Meroitic_Cursive.
pub fn isMeroiticCursive(self: MeroiticCursive, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
