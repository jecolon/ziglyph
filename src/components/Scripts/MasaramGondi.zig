// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Masaram_Gondi code points.

const std = @import("std");
const mem = std.mem;

const MasaramGondi = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 72968,
hi: u21 = 94177,

pub fn init(allocator: *mem.Allocator) !MasaramGondi {
    var instance = MasaramGondi{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 21210),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097144;
    while (index <= 2097150) : (index += 1) {
        instance.array[index] = true;
    }
    index = 0;
    while (index <= 1) : (index += 1) {
        instance.array[index] = true;
    }
    index = 3;
    while (index <= 40) : (index += 1) {
        instance.array[index] = true;
    }
    index = 41;
    while (index <= 46) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[50] = true;
    index = 52;
    while (index <= 53) : (index += 1) {
        instance.array[index] = true;
    }
    index = 55;
    while (index <= 61) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[62] = true;
    instance.array[63] = true;
    index = 72;
    while (index <= 81) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *MasaramGondi) void {
    self.allocator.free(self.array);
}

// isMasaramGondi checks if cp is of the kind Masaram_Gondi.
pub fn isMasaramGondi(self: MasaramGondi, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
