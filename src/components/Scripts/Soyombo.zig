// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Soyombo code points.

const std = @import("std");
const mem = std.mem;

const Soyombo = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 72192,
hi: u21 = 72354,

pub fn init(allocator: *mem.Allocator) !Soyombo {
    var instance = Soyombo{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 163),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    instance.array[80] = true;
    index = 81;
    while (index <= 86) : (index += 1) {
        instance.array[index] = true;
    }
    index = 87;
    while (index <= 88) : (index += 1) {
        instance.array[index] = true;
    }
    index = 89;
    while (index <= 91) : (index += 1) {
        instance.array[index] = true;
    }
    index = 92;
    while (index <= 137) : (index += 1) {
        instance.array[index] = true;
    }
    index = 138;
    while (index <= 150) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[151] = true;
    index = 152;
    while (index <= 153) : (index += 1) {
        instance.array[index] = true;
    }
    index = 154;
    while (index <= 156) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[157] = true;
    index = 158;
    while (index <= 162) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Soyombo) void {
    self.allocator.free(self.array);
}

// isSoyombo checks if cp is of the kind Soyombo.
pub fn isSoyombo(self: Soyombo, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
