// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt by running ucd_gen.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Ol_Chiki code points.

const std = @import("std");
const mem = std.mem;

const OlChiki = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 7258,
hi: u21 = 42507,

pub fn init(allocator: *mem.Allocator) !OlChiki {
    var instance = OlChiki{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 35250),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 2097142;
    while (index <= 2097151) : (index += 1) {
        instance.array[index] = true;
    }
    index = 0;
    while (index <= 29) : (index += 1) {
        instance.array[index] = true;
    }
    index = 30;
    while (index <= 35) : (index += 1) {
        instance.array[index] = true;
    }
    index = 36;
    while (index <= 37) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *OlChiki) void {
    self.allocator.free(self.array);
}

// isOlChiki checks if cp is of the kind Ol_Chiki.
pub fn isOlChiki(self: OlChiki, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
