// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Private Use Area code points.

const std = @import("std");
const mem = std.mem;

const PrivateUseArea = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 57344,
hi: u21 = 63743,

pub fn init(allocator: *mem.Allocator) !PrivateUseArea {
    var instance = PrivateUseArea{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 6400),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 6399) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *PrivateUseArea) void {
    self.allocator.free(self.array);
}

// isPrivateUseArea checks if cp is of the kind Private Use Area.
pub fn isPrivateUseArea(self: PrivateUseArea, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
