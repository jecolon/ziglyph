// Autogenerated from http://www.unicode.org/Public/UCD/latest/ucd/UCD.zip by running ucd_gen.sh.
// Placeholders:
//    0. Code point type
//    1. Struct name
//    2. Array length
//    3. Highest code point
//    4. Lowest code point
//! Unicode Syriac code points.

const std = @import("std");
const mem = std.mem;

const Syriac = @This();

allocator: *mem.Allocator,
array: []bool,
lo: u21 = 1792,
hi: u21 = 2154,

pub fn init(allocator: *mem.Allocator) !Syriac {
    var instance = Syriac{
        .allocator = allocator,
        .array = try allocator.alloc(bool, 363),
    };

    mem.set(bool, instance.array, false);

    var index: u21 = 0;
    index = 0;
    while (index <= 13) : (index += 1) {
        instance.array[index] = true;
    }
    instance.array[15] = true;
    instance.array[16] = true;
    instance.array[17] = true;
    index = 18;
    while (index <= 47) : (index += 1) {
        instance.array[index] = true;
    }
    index = 48;
    while (index <= 74) : (index += 1) {
        instance.array[index] = true;
    }
    index = 77;
    while (index <= 79) : (index += 1) {
        instance.array[index] = true;
    }
    index = 352;
    while (index <= 362) : (index += 1) {
        instance.array[index] = true;
    }

    // Placeholder: 0. Struct name
    return instance;
}

pub fn deinit(self: *Syriac) void {
    self.allocator.free(self.array);
}

// isSyriac checks if cp is of the kind Syriac.
pub fn isSyriac(self: Syriac, cp: u21) bool {
    if (cp < self.lo or cp > self.hi) return false;
    const index = cp - self.lo;
    return if (index >= self.array.len) false else self.array[index];
}
